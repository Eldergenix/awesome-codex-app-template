# apps/mobile AGENTS.md

- This app uses Expo SDK 55 and React Native 0.83.
- NativeWind v5 is pre-release; validate production behavior before release.
- Keep auth tokens in approved secure storage only.
- Use TanStack Query for server state.
- Run `pnpm --filter @repo/mobile typecheck` after code changes.
