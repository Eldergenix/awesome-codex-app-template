name: Packages
properties:
  Package: title
  Owner: people
  Type: select[app, shared, service, tooling]
  Risk: select[low, medium, high]
  VersionPolicy: select[pinned, caret, workspace, manual]
  Notes: rich_text
