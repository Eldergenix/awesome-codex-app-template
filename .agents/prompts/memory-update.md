# Memory Update Prompt

Update `MEMORY.MD` only with durable facts or decisions that should survive across sessions.

Never record:

- secrets or tokens
- private user data
- temporary debugging output
- unsupported assumptions
- customer-specific confidential details

Use an ADR-style note when a decision changes architecture, dependencies, auth, or deployment.
